{
  "name": "lecture-014",
  "version": "1.0.0",
  "description": "Both source and target languages are at the same level of abstraction, for example when we convert TypeScript code to JavaScript code.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel": "babel 00-babel/origin.js -o 00-babel/destination.js -w",
    "tsc": "tsc 02-TSC/index.ts -w"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "socket.io": "^4.5.0",
    "ejs": "^3.1.7",
    "express": "^4.17.3",
    "typescript": "^4.6.3"
  }
}
